# رفع مجلساوي إلى GitHub وRailway

هذه الحزمة تحتوي على **المصدر فقط**. لا تتضمن رمز Discord أو رابط قاعدة البيانات أو أي ملف بيئة أو اعتمادات محلية أو مخرجات بناء.

## رفعها إلى GitHub

فك الضغط أولاً، ثم افتح **المجلد الذي يحتوي `package.json` و`pnpm-lock.yaml` و`client/` و`server/` معاً**. لا ترفع الملفات بصورة متفرقة ولا ترفع ملف ZIP داخل المستودع؛ لأن Railway يحتاج هيكل Node الكامل في جذر المستودع. أنشئ مستودعاً خاصاً في GitHub وارفع محتويات هذا المجلد باستخدام الأوامر التالية بعد استبدال رابط المستودع:

```bash
git init
git add .
git commit -m "Initial Majlsawi source"
git branch -M main
git remote add origin https://github.com/USERNAME/REPOSITORY.git
git push -u origin main
```

## إعداد Railway

أنشئ خدمة من **مستودع GitHub** وليس من قائمة ملفات مفككة، ثم تأكد من أن Railway يعرض `package.json` في جذر الخدمة قبل النشر. أضف خدمة MySQL في المشروع نفسه. استخدم أوامر الخدمة التالية:

| الإعداد | القيمة |
| --- | --- |
| Build Command | `pnpm install --frozen-lockfile && pnpm build` |
| Start Command | `pnpm start` |
| Health / port | يقرأ الخادم متغير `PORT` الذي يوفره Railway تلقائياً. |

أضف هذه المتغيرات في Railway، ولا تضف قيمها إلى GitHub:

| المتغير | الغرض |
| --- | --- |
| `DISCORD_BOT_TOKEN` | رمز بوت Discord من Developer Portal. |
| `DATABASE_URL` | رابط اتصال MySQL كامل. |
| `DASHBOARD_PASSWORD` | كلمة مرور جديدة للوحة التحكم؛ استخدم عبارة قوية وفريدة (يوصى بـ14 حرفاً أو أكثر). |
| `JWT_SECRET` | سلسلة عشوائية طويلة لتوقيع جلسة لوحة التحكم. |
| `ELEVENLABS_API_KEY` | مفتاح خادم ElevenLabs فقط. |
| `ELEVENLABS_VOICE_ID` | معرّف صوت عربي/سعودي مستقل من ElevenLabs. |
| `ELEVENLABS_AGENT_ID` | معرّف وكيل مجلساوي في ElevenLabs للمحادثة الطبيعية داخل الروم. |
| `NODE_ENV` | اجعله `production`. |

بعد تجهيز قاعدة البيانات، طبّق الترحيلات من بيئة متصلة بقاعدة بيانات Railway:

```bash
pnpm drizzle-kit migrate
```

## ما تضبطه لاحقاً من اللوحة

بعد النشر، افتح عنوان Railway وأدخل `DASHBOARD_PASSWORD` في شاشة الدخول. لا تستخدم رابط OAuth خارجي. بعد أن يظهر البوت متصلاً، افتح صفحة **الإشراف والحماية** واختر رتبة وروم السجن، ثم رتب الصلاحيات لكل أمر، بما فيها `/join` و`/leave` و`/say`. افتح صفحة **اللوقات والتوجيه** لربط كل فئة من اللوقات بروم Discord المناسب، ومن ضمنها **تفاعلات البوت**. راجع `README.md` للتفاصيل وحدود الذكاء الاصطناعي الآمنة.

في صفحة **الإشراف والحماية** الصق رابط Railway العام في حقل **رابط لوحة التحكم** ثم احفظ. سيظهر للأشخاص ذوي صلاحية إدارة السيرفر أمر Discord باسم `/dashboard` يرسل بطاقة خاصة مع زر **فتح لوحة التحكم**.

لتفعيل النطق، اختر صوتاً عربياً/سعودياً مستقلاً في ElevenLabs وانسخ معرّفه إلى `ELEVENLABS_VOICE_ID`. لا ترفع أو تستخدم أي مقطع مرجعي، ثم نفّذ `/join` داخل رومك واستخدم `/say` من الدور المخوّل بعد أن ينضم البوت. ولتفعيل السوالف الطبيعية، أضف معرّف وكيل مجلساوي إلى `ELEVENLABS_AGENT_ID`، ثم من اللوحة اختر روم **محادثة مجلساوي المخصص** ورتبة **الموافقة على المحادثة الصوتية**. ادخل الروم بعد إعطاء الرتبة فقط للأشخاص الموافقين، ثم قل «يا مجلساوي». لا يستمع البوت أو يشترك بصوت عضو خارج هذا الروم أو بلا رتبة الموافقة، ولا تسجل النسخة الصوت الخام أو النص الناتج في قاعدة البيانات أو اللوقات.

من صفحة **الإشراف والحماية** تستطيع إضافة Discord User ID إلى **بلاك ليست مجلساوي**. العضو المحظور لا يحصل على رد أو تنفيذ أو معالجة صوتية من البوت، وتُسجَّل عملية الإضافة أو الإزالة في قناة **تفاعلات البوت** إذا ربطتها من اللوحة.
