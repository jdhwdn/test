# دليل تشغيل مجلساوي

هذا الدليل يصف تشغيل **النسخة الآمنة المكتملة** من مجلساوي على Railway. يحمل المشروع لوحة React/Express وبوت Discord في عملية Node واحدة؛ لذلك يجب أن يبقى أمر التشغيل مستمراً حتى تتصل بوابة Discord وتظل Online.

| مرحلة | الإجراء | نتيجة التحقق |
| --- | --- | --- |
| قبل النشر | ارفع محتويات `discord-guardian-railway.zip` إلى مستودع GitHub أو استخدمها مباشرة كمصدر خدمة Railway. | يظهر `package.json` و`pnpm-lock.yaml` في جذر المصدر. |
| المتغيرات | أضف `DISCORD_BOT_TOKEN` و`DATABASE_URL` و`DASHBOARD_PASSWORD` و`JWT_SECRET`. أضف متغيرات ElevenLabs فقط عند تفعيل الصوت المستقل. | لا تحفظ أي قيمة سرية في Git. |
| البناء والتشغيل | استخدم `pnpm install --frozen-lockfile && pnpm build` للبناء و`pnpm start` للتشغيل. | تظهر في سجلات Railway رسالة بدء الخادم ثم `Gateway ready`. |
| قاعدة البيانات | اترك ترحيلات Drizzle المرفقة تعمل عند بدء الخدمة. | ينشأ أو يتحدث سجل `__drizzle_migrations` مرة واحدة لكل مخطط. |
| Discord | فعّل Intents المطلوبة وامنح البوت أقل الصلاحيات التي تحتاجها الأنظمة المفعّلة. | يسجل البوت في السيرفر وتظهر أوامر Slash. |
| اللوحة | افتح عنوان Railway وسجل الدخول باستخدام `DASHBOARD_PASSWORD`. اختر السيرفر واضبط قنوات اللوقات والإعدادات المطلوبة. | تستمر الإعدادات بعد تحديث الصفحة. |

## فحص سريع بعد النشر

تأكد أولاً من أن Deploy Logs تحتوي على عبارة **Gateway ready** باسم البوت وعدد السيرفرات. بعد ذلك جرّب `/dashboard` بحساب لديه **Manage Server**، وحدد قناة لواحدة من فئات اللوقات، ثم نفّذ إجراءً آمناً مثل `/rank` أو `/events` للتحقق من الاستجابة.

اختبر السجن فقط في بيئة خاضعة للإدارة: اضبط رتبة السجن والقناة، وتأكد من أن رتبة البوت أعلى من الرتب التي يفترض سحبها أو إعادتها. لا تمنح البوت أو رتب المتجر صلاحية **Administrator** من أجل تجاوز هذه القيود.

## التحذيرات والطعن

يمكن للمشرف المخوّل عرض التحذيرات النشطة عبر `/warnings` وحل تحذير محدد عبر `/unwarn`. بعد أن يصبح التحذير محلولاً، يستطيع العضو المعني وحده استخدام `/appealwarning id:<رقم> note:<ملاحظة>` داخل السيرفر نفسه. تقبل الملاحظة حتى 600 حرف، وتُحفظ مرة واحدة فقط؛ لا يعرض الرد سبب التحذير أو بياناته ولا ينشر الملاحظة في لوقات Discord.

> يجب أن يراجع فريق الإشراف ملاحظات الطعن داخل قاعدة البيانات أو أدواته الموثوقة فقط. لا توجد في هذه النسخة آلية قرار تلقائي أو إعادة فتح تلقائية للتحذير.

## إنشاء رتبة سجن من Discord

يستطيع المدير استخدام `/adminassist` لطلب خطة سجن محكومة بالمعاينة. اكتب في حقل `request` صيغة مثل: `انشى رتبة اسمها تالف خاصة بالسجن مايشوف الا روم ss`. يعرض مجلساوي اسم الرتبة والروم قبل التنفيذ، ثم يحتاج ضغط **تأكيد آمن** خلال خمس دقائق. عند التأكيد، ينشئ رتبة جديدة بلا صلاحيات إدارية، ويمنعها من رؤية أو الكتابة أو الاتصال في بقية القنوات القابلة للإدارة، ويسمح لها بروم السجن النصي المسمى فقط، ثم يحفظ الرتبة والروم لإجراء `/jail`.

> يحتاج البوت **Manage Roles** و**Manage Channels** في كل القنوات المستهدفة. إذا تعذر عليه إدارة أي قناة، يرفض الخطة قبل إنشاء الرتبة. لا يغير هذا المسار صلاحيات أي رتبة أو عضو قائم.

## حدود مقصودة

تظل مسارات Azure وPiper وVosk، والأخبار، والتذكيرات، والتنظيف الدوري **مؤجلة** إلى أن يختار مالك المشروع مزود الخدمة وسياسة الجدولة وبيئة التشغيل المناسبة. كذلك لا تسجل النسخة محادثات التذاكر، أو الصوت الخام، أو النص الخام الموجّه إلى المساعد في لوقات التدقيق. راجع `completion-scope.md` و`README.md` قبل تمكين أي نظام جديد.
